# 🧩 Effective Mobile — Тестовое задание

Сервис для управления пользователями: регистрация, авторизация, получение данных и блокировка. 
## 🚀 Быстрый старт
1. Клонируйте репо 
```bash
https://github.com/lyoha61/test-em.git
cd test-em
```
2. Настройте переменный окружения 
```
cp .env.example .env
```
3. Запустите сервис
```
docker compose up --build -d 
```
4. Запустить миграции и сиды
```
docker exec -it em_backend npm run deploy 
```
Сервис будет доступен по адресу: http://localhost:3000.

## Endpoints:

### Auth
- POST /api/v1/auth/register — регистрация пользователя
- POST /api/v1/auth/login — логин пользователя
### User

- GET /api/v1/users/ — получить список всех пользователей
- GET /api/v1/users/:id — получить одного пользователя по ID
- PATCH /api/v1/users/:id/block — заблокировать пользователя по ID

## ТЗ
Нужно разработать сервис работы с пользователями. Модель пользователя должна
содержать:
- ФИО
- Дату рождения
- Email - уникальное значение
- Пароль
- Роль - либо admin либо user
- Статус пользователя - активный или нет

Должны быть реализованы следующие endpoint:
1. Регистрация пользователя
2. Авторизация пользователя - любой механизм
3. Получение пользователя по ID (Может получить либо админ либо пользователь сам
себя)
4. Получение списка пользователей - только для админа
5. Блокировка пользователя - либо админ либо пользователь сам себя
